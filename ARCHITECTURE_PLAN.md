Low priority
TUPLES:
- Типизированные паттерны
- destructuring в параметрах функций
- tuple-паттерны в match

VM
- C++ builtins (print, + и т.д.) в файл не сериализуются — при запуске берутся из stdlib интерпретатора
- import при компиляции всё ещё может выполнять модули через интерпретатор
- generic-функции в байткод не попадают

-------------------------------------------------------

- переделать ноды
- регистрация классов математики

- тесты алгоритмы